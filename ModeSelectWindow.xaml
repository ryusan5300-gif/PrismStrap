<Window x:Class="PrismStrap.ModeSelectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PrismStrap" Height="320" Width="460"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        Icon="icon.ico"
        ShowInTaskbar="True"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <!-- Action Card Button Style -->
        <Style x:Key="ActionCardButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1A1C23" />
            <Setter Property="BorderBrush" Value="#2B2E3A" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="CardBorder" CornerRadius="10" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="14,12">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CardBorder" Property="Background" Value="#222530" />
                                <Setter TargetName="CardBorder" Property="BorderBrush" Value="#4B5563" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="CardBorder" Property="Background" Value="#16181F" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Accent Card Style -->
        <Style x:Key="PrimaryActionCardButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionCardButtonStyle}">
            <Setter Property="Background" Value="#141E33" />
            <Setter Property="BorderBrush" Value="#2563EB" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="CardBorder" CornerRadius="10" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="14,12">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CardBorder" Property="Background" Value="#1D2E52" />
                                <Setter TargetName="CardBorder" Property="BorderBrush" Value="#3B82F6" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="CardBorder" Property="Background" Value="#0F172A" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="12" Padding="20,18" Background="#16171D" BorderBrush="#2D303B" BorderThickness="1" CornerRadius="14">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="4" Direction="270" Opacity="0.6" Color="#000000" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Top Header (Icon + Title + Close) -->
            <Grid Grid.Row="0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Image Source="icon.ico" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                    <TextBlock Text="PrismStrap" FontSize="14" FontWeight="Bold" Foreground="#FFFFFF" VerticalAlignment="Center" />
                    <Border Background="#21242D" CornerRadius="4" Padding="6,1" Margin="8,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="v0.3.0" FontSize="9" FontWeight="SemiBold" Foreground="#9CA3AF" />
                    </Border>
                </StackPanel>

                <Button x:Name="BtnClose" Content="✕" Width="24" Height="24" HorizontalAlignment="Right" VerticalAlignment="Center"
                        Background="Transparent" Foreground="#71717A" BorderThickness="0" Cursor="Hand" FontSize="11" Click="BtnClose_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="4">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="bd" Property="Background" Value="#292C36" />
                                                <Setter Property="Foreground" Value="#FFFFFF" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- Subtitle -->
            <TextBlock Grid.Row="1" x:Name="TxtSubtitle" Text="実行する操作を選択してください" FontSize="12" Foreground="#9CA3AF" Margin="0,8,0,14" />

            <!-- Choice Actions -->
            <StackPanel Grid.Row="2" VerticalAlignment="Center">
                <!-- Option 1: Play Roblox -->
                <Button x:Name="BtnPlayRoblox" Style="{StaticResource PrimaryActionCardButtonStyle}" Margin="0,0,0,10" Click="BtnPlayRoblox_Click">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!-- Icon Badge -->
                        <Border Grid.Column="0" Width="36" Height="36" CornerRadius="8" Background="#1D4ED8" Margin="0,0,14,0" VerticalAlignment="Center">
                            <TextBlock Text="▶" FontSize="15" FontWeight="Bold" Foreground="#FFFFFF" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2,0,0,0" />
                        </Border>

                        <!-- Text -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock x:Name="TxtPlayTitle" Text="Roblox をプレイ" FontSize="14" FontWeight="SemiBold" Foreground="#FFFFFF" />
                            <TextBlock x:Name="TxtPlayDesc" Text="最適化とModを適用して起動します" FontSize="11" Foreground="#93C5FD" Margin="0,2,0,0" />
                        </StackPanel>

                        <!-- Arrow -->
                        <TextBlock Grid.Column="2" Text="→" FontSize="16" Foreground="#60A5FA" VerticalAlignment="Center" Margin="6,0,2,0" />
                    </Grid>
                </Button>

                <!-- Option 2: Open Settings -->
                <Button x:Name="BtnOpenSettings" Style="{StaticResource ActionCardButtonStyle}" Click="BtnOpenSettings_Click">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!-- Icon Badge -->
                        <Border Grid.Column="0" Width="36" Height="36" CornerRadius="8" Background="#262933" Margin="0,0,14,0" VerticalAlignment="Center">
                            <TextBlock Text="⚙" FontSize="16" Foreground="#D1D5DB" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>

                        <!-- Text -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock x:Name="TxtSettingsTitle" Text="PrismStrap 設定" FontSize="14" FontWeight="SemiBold" Foreground="#FFFFFF" />
                            <TextBlock x:Name="TxtSettingsDesc" Text="FPS・グラフィック・死亡サウンドを設定" FontSize="11" Foreground="#9CA3AF" Margin="0,2,0,0" />
                        </StackPanel>

                        <!-- Arrow -->
                        <TextBlock Grid.Column="2" Text="→" FontSize="16" Foreground="#6B7280" VerticalAlignment="Center" Margin="6,0,2,0" />
                    </Grid>
                </Button>
            </StackPanel>

            <!-- Row 3: Live Community Activity Stats -->
            <Border Grid.Row="3" Margin="0,12,0,0" Padding="10,6" Background="#111319" BorderBrush="#252834" BorderThickness="1" CornerRadius="8">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse Width="6" Height="6" Fill="#10B981" Margin="0,0,6,0" VerticalAlignment="Center" />
                        <TextBlock x:Name="TxtLiveOnline" Text="プレイ中: 0 人" FontSize="10" Foreground="#10B981" FontWeight="SemiBold" VerticalAlignment="Center" />
                        <TextBlock Text=" • " FontSize="10" Foreground="#4B5563" VerticalAlignment="Center" Margin="2,0" />
                        <TextBlock x:Name="TxtLiveOffline" Text="オフライン: 0 人" FontSize="10" Foreground="#9CA3AF" VerticalAlignment="Center" />
                    </StackPanel>
                    <TextBlock x:Name="TxtLiveDownloads" Text="🚀 0 DL" FontSize="10" Foreground="#60A5FA" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center" />
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
