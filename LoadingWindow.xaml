<Window x:Class="PrismStrap.LoadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PrismStrap" Height="200" Width="440"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        Icon="icon.ico"
        ShowInTaskbar="True"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <!-- Pill Button Style -->
        <Style x:Key="PillButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1F2128" />
            <Setter Property="Foreground" Value="#D4D4D8" />
            <Setter Property="BorderBrush" Value="#2E313D" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,6" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" CornerRadius="8" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#292C36" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="#3F4352" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#1A1C22" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="12" Padding="18,16" Background="#16171D" BorderBrush="#2D303B" BorderThickness="1" CornerRadius="12">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Direction="270" Opacity="0.55" Color="#000000" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Top Header (Icon + Title + Close) -->
            <Grid Grid.Row="0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Image Source="icon.ico" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center" />
                    <TextBlock Text="PrismStrap" FontSize="13" FontWeight="Bold" Foreground="#FFFFFF" VerticalAlignment="Center" />
                    <Border Background="#21242D" CornerRadius="4" Padding="5,1" Margin="8,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="v0.3.0" FontSize="9" FontWeight="SemiBold" Foreground="#9CA3AF" />
                    </Border>
                </StackPanel>

                <Button x:Name="BtnClose" Content="✕" Width="24" Height="24" HorizontalAlignment="Right" VerticalAlignment="Center"
                        Background="Transparent" Foreground="#71717A" BorderThickness="0" Cursor="Hand" FontSize="11" Click="BtnClose_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="4">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="bd" Property="Background" Value="#292C36" />
                                                <Setter Property="Foreground" Value="#FFFFFF" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- Center Content (Status + Progress) -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="2,10,2,8">
                <TextBlock x:Name="TxtLoadingTitle" Text="Roblox を起動しています..." FontSize="14" FontWeight="SemiBold" Foreground="#FFFFFF" />
                <TextBlock x:Name="TxtLoadingDesc" Text="FastFlags とカスタムサウンドを適用中..." FontSize="11" Foreground="#9CA3AF" Margin="0,3,0,10" />

                <!-- Smooth Indeterminate Progress Bar -->
                <ProgressBar x:Name="BarProgress" Height="4" Background="#21242D" Foreground="#2563EB" BorderThickness="0" IsIndeterminate="True" />
            </StackPanel>

            <!-- Bottom Actions -->
            <Grid Grid.Row="2">
                <Button x:Name="BtnCancel" Content="キャンセル" Style="{StaticResource PillButtonStyle}" HorizontalAlignment="Right" Click="BtnCancel_Click" />
            </Grid>
        </Grid>
    </Border>
</Window>
